# Issues
* If the camera is inside a block faces can be highlighted on the inside
this might be fine though depending on how the block placing logic will eventually work

# Todo
* Placing and delting
* Consider moving meshes to the center so they can be easily rotated
* Place blocks in line with normal
* Look into if meshes under nodes can be combined
